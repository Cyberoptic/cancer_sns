<div class="row">
  <div class="columns large-6 large-offset-3 small-12">
    <div class="callout">
      <h3 class="text-center">新規会員登録</h3>
      <div class="callout warning">
        <h6 class="text-center">
          <strong>
            がん患者の配偶者（もしくは恋人）の方のみ<br/>ご登録・ご利用いただけます
          </strong>
        </h6>
      </div>
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= f.error_notification %>

        <div class="form-inputs">
          <%= f.input :email, required: true, placeholder: "メールアドレス" %>
          <%= f.input :password, required: true, placeholder: 'パスワード', hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
          <%= f.input :password_confirmation, required: true, placeholder: 'パスワード（再確認）' %>
        </div>

        <div class="form-actions">
          <%= f.button :submit, "登録する", class: 'button expanded' %>
        </div>

        <div class="callout secondary">
          <p>
            メールが送信されますので、URLをクリックして本登録へ進んでください。<br />
            ※受信/拒否設定をされている方は[info@cancer-partners.com]から受信できるよう設定をお願いします。<br/>
            ※メールが届かない場合は、迷惑メールフォルダの方もご確認ください。
          </p>
        </div>        
      <% end %>

      <%= render "devise/shared/links" %>
    </div><!-- callout -->
  </div><!-- columns -->
</div><!-- row -->