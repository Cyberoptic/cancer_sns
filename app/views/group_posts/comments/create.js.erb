$('#post-<%= @post.id %>-comments').append("<%= escape_javascript render partial: '/shared/comment', locals: {comment: @comment.decorate} %>");
$('.comment_input').val('');

$('#post-<%= @post.id %>-comments').foundation(); 
app.rebind();
