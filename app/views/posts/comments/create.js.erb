$('#post-<%= @post.id %>-comments').html("<%= escape_javascript render partial: '/shared/comment', collection: @post.comments.includes(:user).decorate %>");
$('.comment_input').val('');

$('#post-<%= @post.id %>-comments').foundation(); 
app.rebind();
