$(".callout[data-post-id='<%= @post.id %>']").replaceWith("<%= escape_javascript render partial: 'posts/post', locals: { post: @post.decorate } %>");

$('#edit-post-modal').foundation('close');
$(document).foundation(); 
app.rebind();
