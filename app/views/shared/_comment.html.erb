<% post.comments.each do |comment| %>
			<span class="user-image">
				<%= image_tag comment.user.photo %>
			</span>
    <%= link_to comment.user.nickname, user_path(post.user) %>
    <%= comment.text %><br>
<% end %> 

