<% if current_user.joined?(group) %>
  <%= link_to "Joined", group_group_unmemberships_path(group.id), method: :post, remote: true %>
<% else %>
  <%= link_to "Join", group_group_memberships_path(group.id), method: :post, remote: true %>
<% end %>